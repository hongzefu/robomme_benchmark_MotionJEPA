# 产物入 git 策略（沿用 v3 约定并扩展）：
# - 胜出变体 watershed_barrier 的 preview + summary.json 入 git；
# - 全帧网格拼图（*_grid.png，单张可达数十 MB）不入 git；
# - 落选变体的整目录产物不入 git——它们是锦标赛过程产物（每变体约 74MB），
#   用 README「用法」一节的命令随时可从 v21-16env 现成 h5 重新生成。
outputs/*/
!outputs/watershed_barrier/
outputs/watershed_barrier/*_grid.png
__pycache__/
variants/__pycache__/
