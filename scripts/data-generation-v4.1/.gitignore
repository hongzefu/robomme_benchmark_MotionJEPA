# 产物入 git 策略（2026-08-12 用户拍板改为「一律不入库」）：
# `outputs/` 下的**全部**产物都不进 git——它们都能用 README「用法」一节的命令从 h5 +
# 代码原样重生成，属于可再生中间物，没有入库价值。
#
# ⚠ 这条不只是「以后不加」：v4 / v4.1 / v4.2 三代 outputs 的历史记录已在同日用
#   `git filter-branch --index-filter` 从 `commitV1.13^..HEAD` 共 6 个 commit 里整体清除，
#   仓库体积因此从 281.9 MiB 降到约 145 MiB。此后再把 outputs 加回来等于白清一次，
#   所以这里用整目录忽略，而不是原来那种「只忽略 logs 与 *_grid.png」的白名单式规则。
#
# ⚠ 副作用：reports/ 下的 markdown 用相对路径引用 outputs/ 里的图，本地看正常，
#   在 GitHub 上看会是空图——图请在本地按复现命令重生成后查看。
outputs/
__pycache__/
